<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yorme.fdma">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FDMA">
        <activity
            android:name=".usermanual.UserManualFilipino"
            android:exported="false" />
        <activity android:name=".usermanual.UserManualEnglish" />
        <activity android:name=".changephonenumber.ChangePhoneNumber" />
        <activity android:name=".changepin.ChangePin" />
        <activity
            android:name=".viewlogs.ViewLogs"
            android:screenOrientation="landscape" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>